# Notebook 04 — Cost optimization

See the Markdown explanations directly above each code cell in `notebooks/04_*.ipynb`. The notebook is intentionally thin: unique calculation logic lives in `src/`.
