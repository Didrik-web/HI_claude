# Notebook 02 — Time-series and connection point

See the Markdown explanations directly above each code cell in `notebooks/02_*.ipynb`. The notebook is intentionally thin: unique calculation logic lives in `src/`.
