# Notebook 05 — Loss optimization

See the Markdown explanations directly above each code cell in `notebooks/05_*.ipynb`. The notebook is intentionally thin: unique calculation logic lives in `src/`.
