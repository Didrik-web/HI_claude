# Notebook 03 — Charging infrastructure and uncontrolled charging

See the Markdown explanations directly above each code cell in `notebooks/03_*.ipynb`. The notebook is intentionally thin: unique calculation logic lives in `src/`.
